# Presenter Plan

## 0. 计划身份与状态

| 字段 | 值 |
|---|---|
| project_id |  |
| revision | 1 |
| 计划状态 | 待确认 |
| 创建/更新时间（UTC） |  |
| 当前阶段 | 输入规格 |
| 用户确认摘要 |  |

状态只使用 `待确认`、`已确认`、`进行中`、`需重审`、`暂停`、`完成`。本文件是唯一中枢；不在其他文档维护会改变执行结果的副本。

项目数据位于 `workspace/projects/<project_id>/`；每个 Panel 包和最终 assembly 包都直接位于该项目根目录，并使用唯一文件名（如 `panel-P001.execution-package.json`、`assembly.execution-package.json`）。本计划和创作素材只使用项目内已经存在、用途明确且可由 package-relative URI 引用的位置，不自行创建 `inputs/`、`environment-pack/`、`packages/`、独立 `qc/`、失败候选或恢复日志。

## 1. 输入规格（待确认）

| 项目 | 记录 |
|---|---|
| 口播目的与受众 |  |
| 角色身份锚点 |  |
| 角色图路径/素材语义 |  |
| 语言、地区、发音 |  |
| 声音参考路径/素材语义 |  |
| 目标画幅 |  |
| 其他画幅（需要时另行建立独立序列） | 无 |
| 总时长与段数 |  |
| 360°环境源 |  |
| 用户确认的方向与区域/站位 |  |
| 表演禁区与发布限制 |  |

## 2. 环境 Pack（待确认）

| 字段 | 记录 |
|---|---|
| 源文件 |  |
| ffprobe 尺寸与比例 |  |
| 比例检查 | 2:1，容差 ±5% |
| manifest 路径 |  |
| contact sheet 路径 |  |
| yaw 0 视图 |  |
| yaw 90 视图 |  |
| yaw 180 视图 |  |
| yaw -90 视图 |  |
| pitch/FOV/尺寸 | pitch 0° / FOV 90° / 1024×1024 |
| 区域说明 |  |

## 3. 口语化文案（待确认）

| shot_id | 文案 | 停顿/重音/发音 | 估计时长 | 录音备注 |
|---|---|---|---:|---|
| S001 |  |  |  |  |

## 4. Shot Plan（待确认）

每行是一个独立的 Shot/Panel Contract；时长必须在 4–15 秒。动作预算只选 L0、L1、L2，并在备注中说明为什么没有超载。

| shot_id / Panel | 画幅 | 时长 | 动作预算 | 区域/yaw | 景别/唯一运镜 | 起始姿态 → 结束姿态 | 文案 | 声音尾音 | 连续性输入 → 输出 | 独立声音引用 |
|---|---|---:|---|---|---|---|---|---|---|---|
| P001 |  |  | L0/L1/L2 |  |  |  |  |  |  | 默认/明确要求 |

### 动作预算

- **L0 静态**：站位和镜头基本锁定；只允许呼吸、眨眼、自然口型和一个微表情。
- **L1 轻微交流**：在 L0 基础上最多一个小手势或一次头/视线转移；镜头只做一种轻微变化。
- **L2 单一复杂意图**：允许一次拿取/指向/转身等主动作和一项镜头变化；不叠加第二个道具动作或第二个信息转折。

## 5. H3 提示词 / LFO 执行包（待确认）

| shot_id / Panel | 参考槽位 | H3 提示词路径/正文版本 | package 路径 | validate | 批准文件 SHA-256 | execute 结果 |
|---|---|---|---|---|---|---|
| P001 | ref_image_0/1/2, ref_audio_0 |  |  | 待运行 | 待计算 | 待运行 |

固定绑定：ref_image_0=角色、ref_image_1=全景、ref_image_2=方向视图、ref_audio_0=声音；从第二个同画幅 Panel 开始，按 operation 需要绑定上一条 `ACCEPT` 片的真实输出为 ref_video_0。H3 标签对应 `<Picture 1/2/3>`、`<Audio 1>`、`<Video 1>`。包变化时重新构建并重新取得 hash，不维护独立 lock/manifest。

## 6. Panel 执行与接力（进行中）

| Panel | package | 批准文件 SHA-256 | execute 结果 | 输出路径 | QC（ACCEPT/REJECT） | 真实尾帧路径（需要时） | 备注 |
|---|---|---|---|---|---|---|---|
| P001 |  |  | 待运行 |  | 待检查 |  |  |

按 P001、P002……严格串行运行。每个 Panel 作为独立的短生命周期执行单元同步等待官方 `comfy-cli` 完成；下一条 Presenter Panel 只接收上一条完整 `ACCEPT` 视频作为 `ref_video_0`，只有其他下游 operation 明确需要精确首帧时才提取真实尾帧。`ERROR` 或 `REJECT` 立即停止，不自动重试，不维护失败候选或恢复记录。

## 7. 最终组装（待确认）

| 项目 | 记录 |
|---|---|
| 所有 Panel | 全部 `ACCEPT` 后组装 |
| assembly package |  |
| video backend | `video.passthrough` |
| validate | 待运行 |
| 批准文件 SHA-256 | 待计算 |
| execute 结果 | 待运行 |
| 最终输出 |  |

## 确认日志

| 时间（UTC） | 阶段 | 用户确认原文/摘要 | revision |
|---|---|---|---:|
|  |  |  | 1 |



