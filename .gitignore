# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# LFO runtime artifacts
end_frames/
normalized/
pipeline_output/
artifacts/
*.db
*.db-wal
*.db-shm
.mimocode/
.pytest_cache/
.cron-lock

# User workspace — never committed. The system (code + templates) lives
# in this repo; the workspace is where novels, storyboards, and run
# outputs live. See workspace/README.md.
workspace/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Local secrets / env
.env
.env.local

# Agent scratch
.superpowers/

.codex-mimo/
pipeline_run.log

# Throwaway debug scripts (created during one-off investigations, not part of the toolkit).
# Add a real script under scripts/ with a meaningful name when it graduates from debugging.
scripts/check_*.py
scripts/clear_*.py
scripts/fix_*.py
scripts/test_*.py
scripts/trace_*.py
scripts/update_*.py

# Temporary prompt / scratch text files at the repo root
tmp_*.txt

# Throwaway storyboard image at the repo root
/storyboard-bw-*.jpg

# Throwaway scratch dir at the repo root (one-off debug, log dumps, prompt drafts)
TEMP/

# Per-project novel/storyboard/user data — analogue to workspace/, never committed
.short-drama/

# Host workbench memory — runtime state, not part of the repo
.workbuddy/
