# GPT-6 适配变更说明

2026-09-07：复审入口、按需引用、模板、元数据与本地验证环境的一致性。本记录说明发现和验证证据，不增加常驻规则或新的审批步骤。

# AI 配置第三轮复审

本轮沿用[前两轮的官方依据与审计范围](ai-config-audit.md)，重点检查精简入口后，旧要求是否还会从引用文件重新进入流程。下表是静态检查确认的规则冲突及可能后果，不表示每项都已在实际模型输出中复现。

| 发现 | 修正 |
| --- | --- |
| 根规则仍使用系统 `python`；本机该解释器没有 pytest，项目 `.venv` 却已可用 | 统一优先使用项目解释器，引用现有隔离临时目录的验证方法 |
| 说人话的第二次回读、无源论断规则和示例可能越过 `in-place` / `bounded` 范围；旧示例仍有“待确认”关卡 | 两次检查受同一 scope 约束；保句数时不删、并、拆句；无源论断不通过删除来源包装成事实，处理方式服从原改写范围 |
| 短剧引用把全套目录写成默认创建，并用系列状态示例暗示长篇创作 | 只在项目保存/续写任务中维护必要文件；对话成稿不初始化项目、不读取其他剧目状态 |
| 短剧桥接模板把 1–2 场景、2–3 角色、15–60 秒等建议写成限制 | 用户给定的篇幅、场景、角色、画幅和逐字对白要求优先；常见预算及 JSON 占位不作硬上限 |
| MG 的旧表格按参考数量猜模式，有下一 Panel 就要求交接尾帧 | 按已确定 operation 和参考用途绑定；只有需要精确首帧的下游才提取真实末帧 |
| 视频拆解正文允许自主选择路线，元数据仍要求用户先选模型 | 元数据与正文统一：只在请求复刻提示词时选路线，已有指定直接沿用，未指定时合理选择一个支持的路线 |
| 视频拆解的独立中文提示词与 zero-to-story 的标准 H3 交接边界不清 | 转入该生产流程时交接证据、创作事实和素材，由 `h3-prompt-writing` 输出包内所需文本；不直接复制分析报告或改变运行时格式要求 |
| 数字人入口列出完整生产流程，最终检查也默认要求包/hash | 按用户请求阶段执行和验收；仅当空间判断确有依赖时准备环境 Pack，文案/提示词交付不附带执行包审批 |
| 外部技能与工具入口采用前轮会话状态，容易被当作当前能力 | 活跃清单更新为当前 25 个 Skill，保留前轮 29 个的历史审计；工具以当前宿主文档为准，不把清单变化当作卸载结果 |

## 保留的边界

项目系统提示词的自主推进、授权复用、主 Agent 验收和局部阻塞规则没有发现新的冲突，本轮无需再叠加一套协作要求。“用户当前指令优先”仍限于项目可配置规则，不能提升实际平台或工具权限。

LFO 的精确文件字节 hash 批准、单 Panel 隔离执行、串行生成、失败停止和 ACCEPT 后组装规则保留。创作资产的失败只阻断它的依赖链；现有入口与详细引用已经规定这一范围，未将其另列为新缺陷。

## 验证范围

独立复审已重新运行相关 117 项测试，全部通过；配置校验器与测试文件的 Ruff 检查、环境准备脚本的 PowerShell 语法检查通过。没有重新安装环境或改动这些脚本。复审还独立检查了当时的 26 个本地 Markdown 锚点，全部有效；这项检查与只验证文件目标的配置校验器分开记录。

最终应用 18 个 Skill 文件：写入前逐项核对基线与审阅稿 SHA-256，写入后全部一致，其余快照 Skill 文件未变化。10 个项目 Skill 的官方 `quick_validate.py` 全部通过；修改文件中的 70 个内联本地文件链接均有目标。

另外完成两次独立的真实文字任务：要求原文三句的数量和顺序不变，保留培训、试点、张岚及全部数字，只交成稿。首次满足保真和句数，但把无源“研究表明”原样保留。随后明确 `audit-only` 在只交成稿时将来源缺口写入原句，再做一次定向复测：仍保留三句及全部具体信息，并以“有研究声称……但未提供出处”表达，没有增加提问或附加报告。[两次原文及检查记录](evaluations/2026-09-07-ai-config-rereview.json)均保留，首次结果未被覆盖。

这两次只是单样本观察，不能证明总体效果或保证所有创作输出均符合预期；[此前真实文字任务中的语义偏差](ai-config-evaluation.md#本轮结果)仍作为已知观察保留。

本轮不更改运行时代码、真实 workspace、用户历史评测、外部 Skill 源文件或插件缓存，不提交媒体生成。仓库中其他未提交改动保留；上述 117 项测试只说明测试时相关范围的结果，不作为其他并行运行时修改的验收。
